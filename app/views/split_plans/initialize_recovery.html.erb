<h1 class="mb-4">When did you last train these muscles?</h1>
<p class="text-muted mb-4">
  Enter the last date you trained each muscle group. This ensures accurate recovery tracking from day one.
  <strong>All dates are required.</strong>
</p>

<%= form_with url: submit_recovery_split_plan_path(@split_plan), method: :post, local: true do %>
  <% @muscles.each do |muscle| %>
    <div class="mb-3">
      <label class="form-label"><%= AppConstants::LABELS[muscle] %> *</label>
      <%= date_field_tag "recovery_dates[#{muscle}]", nil,
                         class: "form-control",
                         required: true,
                         max: Date.today %>
    </div>
  <% end %>

  <div class="mt-4">
    <%= submit_tag "Initialize Recovery Tracking", class: "btn btn-primary" %>
    <!-- REMOVED: Skip button -->
  </div>
<% end %>
